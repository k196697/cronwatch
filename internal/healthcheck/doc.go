// Package healthcheck provides a lightweight HTTP server that exposes
// /healthz and /status endpoints for inspecting the live state of
// monitored cron jobs based on collected metrics.
package healthcheck
